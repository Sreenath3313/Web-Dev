const div = document.createElement('div');
div.id = 'myDiv';
document.body.appendChild(div);

// Get the element by id
const element = document.getElementById('myDiv');
console.log(element); // Logs the created div element